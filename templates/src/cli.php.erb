<?php

require_once("includes/bootstrap.php");

switch($argv[1]) {
	default:
		echo "usage: {$argv[0]} (input|worker|output)\n";
		exit(1);
		break;

	case "input":
		\<%=params[:namespace]%>\Input::run();
		break;

	case "worker":
		\<%=params[:namespace]%>\Worker::run();
		break;

	case "output":
		\<%=params[:namespace]%>\Output::run();
		break;
}
